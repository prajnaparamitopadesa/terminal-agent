{
  "name": "terminal-agent",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --cwd frontend dev & bun run --cwd backend dev",
    "start": "concurrently \"npm run build --prefix frontend\" \"npm run start --prefix backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
